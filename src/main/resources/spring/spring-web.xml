<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd">
    <!-- SpringMVC Configuration -->
    <!-- 1. Enable SpringMVC Annotation Mode -->
    <!-- Simplified configuration:
    (1) Automatically registers DefaultAnnotationHandlerMapping and AnnotationMethodHandlerAdapter
    (2) Provides built-in support for: data binding, number/date formatting (@NumberFormat, @DateTimeFormat), default XML/JSON read-write support
    -->
    <mvc:annotation-driven/>

    <!-- 2. Default Servlet Configuration for Static Resources
    (1) Enables handling of static resources: js, gif, png
    (2) Allows using "/" for global mapping
    -->
    <mvc:default-servlet-handler/>

    <!-- 3. Configure JSP ViewResolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView" />
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- 4. Scan for Web-related Beans -->
    <context:component-scan base-package="org.seckill.web" />
</beans>