<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--Global Properties Configuration  -->
    <settings>
        <!-- Use JDBC's getGeneratedKeys to retrieve database auto-incremented primary key values -->
        <setting name="useGeneratedKeys" value="true" />

        <!-- Use column aliases instead of column names. Default: true -->
        <setting name="useColumnLabel" value="true" />

        <!-- Enable camelCase naming conversion: Table{create_time} -> Entity{createTime} -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
</configuration>